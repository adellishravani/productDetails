const SimilarProductItem = props => {
  const {similarDetails} = props
  const {
    id,
    imageUrl,
    title,
    style,
    price,
    description,
    brand,
    totalReviews,
    rating,
    availability,
  } = similarDetails
}

export default SimilarProductItem
